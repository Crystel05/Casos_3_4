package RedSocialTest.Responses;

import Network.Response.IResponse;

public class ConnectionResponse implements IResponse {

    //Todos tienen un tipo que tendria que ser seteado en su constructor. O con un setType

    @Override
    public Enum getType() {
        return null;
    }
}
